/*Given a non-negative integer - N, print the sum of digits of the given number.

Input Format

First and only line of input contains a non-negative integer N.

Constraints

0 <= length(N) <= 10^3 =>a mumber with 1000 digits => eg length(142523) = 6 , hence to get length of 10^3, N=10^10^3

Output Format

Print the sum of digits of the given number.*/


#include <cmath>
#include <cstdio>
#include <vector>
#include <string>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {

    string str;
    int sum = 0,b;
    string c;
    cin>> str;
    int a = str.length();
    for(int i=0; i<a;i++)
    {
       c = str.at(i);
       b = stoi(c); 
       sum = sum+b;
    }  
    cout<<sum;
    
    return 0;
}


Input:
123456789123456789012345678901234567890123456789012345678

Output:
522

